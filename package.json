{
  "name": "kazoku-tsuwa",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "server": "node server/signaling.mjs",
    "start": "concurrently -k -n web,sig -c cyan,green \"npm:dev\" \"npm:server\"",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.0",
    "@capacitor/core": "^8.4.0",
    "firebase": "^12.14.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.0",
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3",
    "vite": "^6.0.7"
  }
}
